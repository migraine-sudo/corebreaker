<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.poc.deviceconfigleak">

    <!--
        V-415: SettingsProvider DeviceConfig Zero-Permission Read PoC

        PERMISSIONS REQUIRED: NONE
        No permissions declared — this PoC demonstrates that DeviceConfig flags
        are readable by ANY installed app without READ_DEVICE_CONFIG permission.
    -->

    <uses-sdk android:minSdkVersion="29" android:targetSdkVersion="35" />

    <application
        android:label="DeviceConfig Leak PoC"
        android:icon="@android:drawable/ic_dialog_info"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="DeviceConfig Leak PoC">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
